import 'package:flutter/material.dart';

class StoreHomeTab extends StatelessWidget {
  const StoreHomeTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('홈 탭 준비 중입니다.'),
    );
  }
}
