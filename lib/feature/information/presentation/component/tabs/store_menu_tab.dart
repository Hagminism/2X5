import 'package:flutter/material.dart';

class StoreMenuTab extends StatelessWidget {
  const StoreMenuTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('메뉴 탭 준비 중입니다.'),
    );
  }
}
