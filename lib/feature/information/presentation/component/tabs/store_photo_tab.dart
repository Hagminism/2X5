import 'package:flutter/material.dart';

class StorePhotoTab extends StatelessWidget {
  const StorePhotoTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('사진 탭 준비 중입니다.'),
    );
  }
}
