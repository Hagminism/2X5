import 'package:flutter/material.dart';

class StoreInfoTab extends StatelessWidget {
  const StoreInfoTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('정보 탭 준비 중입니다.'),
    );
  }
}
